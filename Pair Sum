int pairSum(int *input, int size, int x)
{
	int i,j,ans=0;
    for(i=0;i<size-1;i++)
    {
        for(j=i+1;j<size;j++)
            if(input[i]+input[j]==x)
                ans++;
    }
    return ans;
}
